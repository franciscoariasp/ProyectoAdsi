@model IEnumerable<ProyectoAdsi.Models.proveedor>

<select class="form-control" id="ir_Proveedor"name="id_proveedor">
    @foreach (var item in Model) 

    {
    
                    <option value="@item.id">@item.nombre</option>

    }
</select>